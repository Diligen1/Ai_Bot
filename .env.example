BINANCE_API_KEY=
BINANCE_API_SECRET=
OPENAI_API_KEY=
TESTNET=true

# AI Analyzer V1 (advisory only, virtual/paper trading — see ai/analyzer.py).
# Disabled by default even if a key is set below; must be explicitly enabled.
AI_ENABLED=false
GEMINI_API_KEY=
